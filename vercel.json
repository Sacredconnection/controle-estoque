{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["gru1"],
  "functions": {
    "app.py": {
      "maxDuration": 60,
      "excludeFiles": "{tests/**,instance/**,.git/**,.vscode/**,render.yaml,Dockerfile,.dockerignore}"
    }
  }
}
